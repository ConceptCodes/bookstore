# Model provider — Vercel AI Gateway (default Eve model routes through this)
AI_GATEWAY_API_KEY=

# Alternative: pull a token via `vercel link` instead of an explicit key
# VERCEL_OIDC_TOKEN=

# Alternative: direct Anthropic provider (set model in agent.ts if used)
# ANTHROPIC_API_KEY=
